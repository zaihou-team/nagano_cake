<div class="container mt-4">
  <h3 class="fw-bold text-center py-2" style="background-color: #f8f9fa; display: inline-block; padding: 10px 30px; border-radius: 5px;">商品詳細</h3>

  <div class="d-flex justify-content-center">
    <div class="d-flex align-items-start w-75">

      <div class="me-5">
        <div class="border p-3 bg-light d-flex align-items-center justify-content-center" style="width: 250px; height: 250px;">
          <%= image_tag @item.get_image %>
        </div>
      </div>

      <div class="flex-grow-1">
        <div class="mb-4">
          <span class="fw-bold">商品名</span><br>
          <span><%= @item.name %></span>
        </div>

        <div class="mb-4">
          <span class="fw-bold">商品説明</span><br>
          <span><%= @item.introduction %></span>
        </div>

        <div class="mb-4">
          <span class="fw-bold">ジャンル</span><br>
          <span><%= @item.genre.genre_name %></span>
        </div>

        <div class="mb-4">
          <span class="fw-bold">税込価格（税抜価格）</span><br>
          <span><%= (@item.price * 1.1).round.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>) 円</span>
        </div>

        <div class="mb-4">
          <span class="fw-bold">販売ステータス</span><br>
          <span class="<%= @item.is_active ? 'text-success fw-bold' : 'text-secondary fw-bold' %>">
            <%= @item.is_active ? '販売中' : '販売停止' %>
          </span>
        </div>
      </div>
    </div>
  </div>

  <div class="text-center mt-4">
    <%= link_to '編集する', edit_admin_item_path(@item.id), class: "btn btn-success px-4 py-2" %>
  </div>
</div>
